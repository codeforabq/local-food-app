//shopping cart functions

//once sessions is established be sure to link it here

module.exports = {

	//need to establish name will be the username and need to link with sessions
	//so the shopping cart belongs to a real person

	getShoppingCart: function(item) {
		var add, remove, checkOut;
},


	add: function (price, quantity) {
		var newItem = newShoppingCart
	}

}; //end module.exports object
